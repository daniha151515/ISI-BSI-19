{% extends 'base.html.twig' %}

{% block title %}Editar Mesa{% endblock %}

{% block body %}
    <div class="row justify-content-center">
    <h1>Editar mesa</h1>
    </div>
    {{ include('mesa/_form.html.twig', {'button_label': 'Actualizar'}) }}
    <div class="row justify-content-center">
    <a href="{{ path('mesa_index') }}">volver a lista</a>
    </div>
    {{ include('mesa/_delete_form.html.twig') }}
{% endblock %}
