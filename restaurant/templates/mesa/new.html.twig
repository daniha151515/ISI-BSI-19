{% extends 'base.html.twig' %}

{% block title %}Nueva Mesa{% endblock %}

{% block body %}
    <div class="row justify-content-center">
    <h1>Crear nueva mesa</h1>
    </div>
    {{ include('mesa/_form.html.twig') }}
    <div class="row justify-content-center">
    <div class="form-group">
    <a href="{{ path('mesa_index') }}">volver a la lista</a>
    </div>
    </div>
{% endblock %}
