<form method="post" action="{{ path('salonero_delete', {'id': salonero.id}) }}" onsubmit="return confirm('Seguro?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salonero.id) }}">
    <div class="row justify-content-center">
    <button class="btn btn-danger">Borrar</button>
    </div>
</form>
